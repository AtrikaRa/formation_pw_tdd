# on: push
# name: Slack Notification Demo
# jobs:
#   slackNotification:
#     name: Slack Notification
#     runs-on: ubuntu-latest
#     steps:
#     - uses: actions/checkout@v2
#     - name: Slack Notification
#       uses: rtCamp/action-slack-notify@v2
#       env:
#         VAULT_ADDR: ${{ secrets.SLACK_WEBHOOK_URL }}
#         VAULT_TOKEN: ${{ secrets.SLACK_WEBHOOK_URL }}

on: push
name: Unit test
jobs:
  slackNotification:
    name: Slack Notification
    runs-on: ubuntu-latest
    steps:
    - name: Check out repository code
      uses: actions/checkout@v3
    - run: |
        php bin/phpunit
        echo "TEST_STATUS"